import React from "react";

const HireDetail = ({ slug }: { slug: string | undefined }) => {
    return (
        <>
            <div>
                thisis {slug} detail
            </div>
        </>
    )
}

export default HireDetail