import React from "react";

const ItvDetail = ({ slug }: { slug?: string }) => {
    return (
        <>
            this is interview tip {slug} detail page
        </>
    )
}

export default ItvDetail