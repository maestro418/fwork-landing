import React from "react";

const DevDetail = ({ id }: { id: string }) => {
    return (
        <>
            <div>
                this is {id} dev detail page
            </div>
        </>
    )
}

export default DevDetail